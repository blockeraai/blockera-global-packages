## Unreleased

### Development Notes
- Jest maps `@wordpress/annotations` to a stub so suites that import
  `@blockera/editor` search-replace do not need that WordPress package in
  node_modules.

## 1.0.0 (2024-12-08)
