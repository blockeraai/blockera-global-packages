# Template — Blockera plugin is the base. Copy to consumer and change OWNER/REPO.
# Other products: set SPECS_ROOTS / SPECS_PATH / SPECS_DEST / USE_CREATE_WP_ENV (no product recipes).
name: Build Blockera Plugin Tests

on:
    pull_request:
        types: [opened, synchronize, ready_for_review]
    workflow_dispatch:

concurrency:
    group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.head_ref || github.sha }}
    cancel-in-progress: true

jobs:
    pr-workflow-gate:
        name: PR workflow filter
        runs-on: ubuntu-latest
        outputs:
            run: ${{ steps.gate.outputs.run }}
        steps:
            - uses: actions/checkout@v5
              with:
                  token: ${{ secrets.BLOCKERABOT_PAT }}

            - name: Ensure global-packages submodule
              uses: ./.github/actions/ensure-global-packages

            - id: gate
              uses: ./packages/global-packages/packages/dev-tools/github/actions/pr-workflow-gate
              with:
                  workflow-file: build-plugin-zip-tests.yml

    build_tests:
        name: Building and Run E2E Tests (PHP ${{ matrix.php }})
        needs: pr-workflow-gate
        if: ${{ needs.pr-workflow-gate.outputs.run == 'true' && (github.repository == 'OWNER/REPO' || github.event_name == 'pull_request') }}
        runs-on: ubuntu-latest
        strategy:
            fail-fast: true
            matrix:
                php: ['7.4', '8.0', '8.1', '8.2']
        env:
            BLOCKERA_GLOBAL_PACKAGES_TOKEN: ${{ secrets.BLOCKERABOT_PAT }}
        steps:
            - uses: actions/checkout@v5
              with:
                  token: ${{ secrets.BLOCKERABOT_PAT }}

            - name: Ensure global-packages submodule
              uses: ./.github/actions/ensure-global-packages

            - uses: ./packages/global-packages/packages/dev-tools/github/actions/build-plugin-zip-tests
              with:
                  php-version: ${{ matrix.php }}
                  # skip-if-no-specs: 'true'
                  # zip-file: blockera-pro.zip
                  # build-dir: ./build/blockera-pro
